# Flask 웹 애플리케이션 의존성

# 핵심 Flask 프레임워크
Flask==3.0.0

# Flask가 의존하는 필수 패키지들 (자동으로 설치되지만 명시적으로 표시)
Werkzeug==3.0.1
Jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.7

# 개발 및 디버깅용 (선택사항)
python-dotenv==1.0.0

# 프로덕션 배포용 WSGI 서버 (선택사항)
# gunicorn==21.2.0
# waitress==2.1.2