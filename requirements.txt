# 로또 예측 시스템 - 패키지 의존성
# 설치 방법: pip install -r requirements.txt

# === 핵심 데이터 처리 ===
pandas>=1.5.0              # 데이터프레임 및 CSV 처리
numpy>=1.21.0              # 수치 계산 및 배열 처리

# === 기계학습 및 통계 ===
scikit-learn>=1.1.0        # 머신러닝 알고리즘 (추후 확장용)
scipy>=1.9.0               # 과학 계산 및 통계 함수

# === 시각화 (선택적) ===
matplotlib>=3.5.0          # 기본 플롯
seaborn>=0.11.0            # 통계 시각화

# === 웹 인터페이스 (추후 확장용) ===
flask>=2.0.0               # 웹 프레임워크
jinja2>=3.0.0              # 템플릿 엔진

# === 설정 및 데이터 형식 ===
pyyaml>=6.0                # YAML 설정 파일 처리
python-dateutil>=2.8.0     # 날짜 처리

# === 시스템 모니터링 ===
psutil>=5.8.0              # 시스템 정보 (CPU, 메모리) - 누락된 패키지 추가

# === 개발 및 테스트 (선택적) ===
pytest>=7.0.0              # 테스트 프레임워크
pytest-cov>=3.0.0          # 테스트 커버리지

# === 취약점 개선 ===
setuptools>=70.0.0

# === Python 버전 요구사항 ===
# Python>=3.10

# === 선택적 패키지 (고급 기능용) ===
# jupyter>=1.0.0           # 노트북 환경
# plotly>=5.0.0            # 인터랙티브 시각화  
# dash>=2.0.0              # 웹 대시보드
# streamlit>=1.0.0         # 간단한 웹앱

# === 성능 최적화 (선택적) ===
# numba>=0.56.0            # JIT 컴파일러
# joblib>=1.1.0            # 병렬 처리